/***************************************************** JAVA ******************************************************/

Optimal Solution - We simulate digit-by-digit addition like elementary math by traversing both linked lists, carrying over values 
                    greater than 9, and building the result list using a dummy head.

class Solution {
    public ListNode addTwoNumbers(ListNode l1, ListNode l2) {
        // Dummy node to simplify result list construction
        ListNode res = new ListNode(0);
        ListNode ptr = res;
        int carry = 0;
        // Traverse both lists until both are exhausted
        while (l1 != null || l2 != null) {
            // Start with carry from previous digit
            int sum = carry;
            // Add value from l1 if available
            if (l1 != null) {
                sum += l1.val;
                l1 = l1.next;
            }
            // Add value from l2 if available
            if (l2 != null) {
                sum += l2.val;
                l2 = l2.next;
            }
            // Update carry for next iteration
            carry = sum / 10;
            // Current digit value
            sum = sum % 10;
            // Append new node to result list
            ptr.next = new ListNode(sum);
            ptr = ptr.next;
        }
        // If carry remains, append it as a new node
        if (carry == 1) {
            ptr.next = new ListNode(1);
        }
        // Return actual head (skip dummy node)
        return res.next;
    }
}

Time Complexity :- O(max(m ,n))
Space Complexity :- O(max(m ,n))
